# Copy to .env and fill in. Never commit .env.
# Used by: cinemawala_apis/functions, cinemawala_aws (Node backends)

# Option A: Path to service account JSON (absolute or relative to project root)
# GOOGLE_APPLICATION_CREDENTIALS=./cinemawala_apis/functions/b1d668fbd6.json

# Option B: Full service account JSON as a single line (escape newlines as \n)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Optional: override storage bucket
# FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
